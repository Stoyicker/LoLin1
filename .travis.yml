language: android
android:
  components:
    - build-tools-21.0.2
before install:
  - echo "stub keystore" > lolin1.keystore
  - echo "stub storage pwd" > lolin1storage.pwd
  - echo "stub key pwd" > lolin1key.pwd
  - chmod +x gradlew
script:
  - ./gradlew assembleGooglePlayDebug
